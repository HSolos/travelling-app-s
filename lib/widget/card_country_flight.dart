import 'package:flutter/material.dart';

class CardCountryFlight extends StatelessWidget {
  const CardCountryFlight({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'CardCountryFlight is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
